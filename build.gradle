/**
 * build.gradle file for TextProcessor
 * 
 * 2019 Steve Neale <steveneale3000@gmail.com>
 */

description = """
'${project.name}'
--- Process text in given languages.
"""

apply plugin: "java"

repositories {
	mavenCentral()
}

dependencies {
	compile fileTree(dir: "libs", include: ["*.jar"])
	compile group: 'commons-cli', name: 'commons-cli', version: '1.2'
	compile group: 'org.apache.opennlp', name: 'opennlp-tools', version: '1.9.1'
	testCompile group: "junit", name: "junit", version: "4.12"
}

jar {
	manifest {
		attributes 'Main-Class': "com.steveneale.textprocessor.TextProcessor"
	}
	from {
		configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
	}
}